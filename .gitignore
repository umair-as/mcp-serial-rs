/target

# Hardware-specific working copy — commit devices.toml.example instead.
devices.toml

# Agent scratch markers (codex, etc.)
.codex

# Environment files (must never be committed).
.env
.env.*

# Editor / OS scratch.
.idea/
.vscode/
*.swp
.DS_Store

# Coverage artifacts.
*.profraw
scratch/**
